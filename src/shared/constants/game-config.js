// GAME CONFIGURATION CONSTANTS PLACEHOLDER
// All game balance and configuration values

// WHAT GOES HERE:

/*
// ENCOUNTER CONFIGURATION
export const ENCOUNTER_CONFIG = {
  // How many unique websites must be visited to trigger 1 encounter
  sitesPerEncounter: 5,
  
  // Maximum encounters that can stack up
  maxEncountersAvailable: 5,
  
  // Rarity weights for random selection
  rarityWeights: {
    common: 60,      // 60% chance
    uncommon: 25,    // 25% chance
    rare: 12,        // 12% chance
    legendary: 3     // 3% chance
  }
};

// CATCH CONFIGURATION
export const CATCH_CONFIG = {
  // Starting number of Pokéballs
  startingPokeballs: 20,
  
  // Pokéballs earned per X Pokemon caught
  pokeballsPerMilestone: 5,
  milestoneInterval: 10, // Every 10 Pokemon = 5 free balls
  
  // Catch rate modifiers
  levelModifier: 0.005,    // Higher level = harder (-0.5% per level)
  rarityModifiers: {
    common: 1.0,           // No change
    uncommon: 0.85,        // -15%
    rare: 0.65,            // -35%
    legendary: 0.35        // -65%
  },
  
  // Catch rate bounds
  minCatchRate: 0.05,      // Always at least 5% chance
  maxCatchRate: 0.95       // Never guaranteed
};

// UI CONFIGURATION
export const UI_CONFIG = {
  // Popup dimensions
  popupWidth: 360,
  popupHeight: 640,
  
  // Animation durations (ms)
  encounterAppearDuration: 600,
  pokeballThrowDuration: 1200,
  catchSuccessDuration: 2000,
  catchFailDuration: 1500,
  
  // Sound settings
  defaultVolume: 0.5,
  soundEnabled: true
};

// SPRITE CONFIGURATION
export const SPRITE_CONFIG = {
  // Sprite sources in priority order
  sources: ['local', 'showdown', 'pokeapi'],
  
  // Local sprite path
  localPath: 'assets/sprites/',
  
  // CDN URLs
  showdownCDN: 'https://play.pokemonshowdown.com/sprites/gen5/',
  pokeApiBase: 'https://pokeapi.co/api/v2/pokemon/',
  
  // Fallback sprite
  fallbackSprite: 'assets/sprites/pokeball.png'
};

// STORAGE KEYS
export const STORAGE_KEYS = {
  player: 'player_data',
  collection: 'pokemon_collection',
  settings: 'game_settings',
  encounterHistory: 'encounter_history'
};

// DEFAULT DATA
export const DEFAULT_PLAYER_DATA = {
  pokeballs: 20,
  totalCaught: 0,
  encountersAvailable: 1, // Start with 1 free encounter
  websitesVisited: 0
};

export const DEFAULT_SETTINGS = {
  soundEnabled: true,
  notificationsEnabled: true,
  animationsEnabled: true
};

// NOTIFICATION CONFIGURATION
export const NOTIFICATION_CONFIG = {
  encounterTitle: 'A wild Pokémon appeared!',
  rareEncounterTitle: '✨ A rare Pokémon appeared!',
  legendaryEncounterTitle: '⭐ A LEGENDARY Pokémon appeared!',
  duration: 5000 // 5 seconds
};
*/
